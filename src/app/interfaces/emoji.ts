interface EmojiInterface {
    id: string[]; // Array von IDs, die die Benutzer repräsentieren, die dieses Emoji verwendet haben
    name: string[]; // Array von Namen der Benutzer
    count: number; 
    emoji: string
  }